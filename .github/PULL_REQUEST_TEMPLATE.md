## What & why

<!-- One focused change. What does it do and what problem does it solve? -->

## Checklist

- [ ] `bin/glassmith demo` still assembles, previews and validates
- [ ] Schema docs (`schema/`) and `README.md` updated if behaviour changed
- [ ] No build output committed (see `.gitignore`)
- [ ] Added/updated an `examples/` fixture for new spec capabilities
